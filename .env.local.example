# Local development against the shared Convex dev deployment.
# Copy this file to `.env.local` before running locally:
#
#   cp .env.local.example .env.local
#
# These values are public deployment identifiers/URLs, not secrets.

CONVEX_DEPLOYMENT=dev:abundant-jaguar-978 # team: viral-sangani, project: titan-os
VITE_CONVEX_URL=https://abundant-jaguar-978.convex.cloud
VITE_CONVEX_SITE_URL=https://abundant-jaguar-978.convex.site

# Telegram app identity for the GTM broadcast feature. Register an app at
# https://my.telegram.org/apps (with the account that will broadcast) and paste
# its api_id / api_hash here. These are APP identity, not account credentials —
# like Telegram's own web client, they ship in the client bundle. The account
# session itself is never here; it stays encrypted on the device.
VITE_TG_API_ID=
VITE_TG_API_HASH=
